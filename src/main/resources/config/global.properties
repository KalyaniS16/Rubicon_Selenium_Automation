# Shared settings
environment=stage
browser = chrome
#browser = firefox
#browser = edge
explicit.wait=20
# Used on Windows locally. Linux/EC2 without a DISPLAY auto-enables headless.
# Override: mvn test -Dheadless=true  or  mvn test -Dheadless=false
headless=false